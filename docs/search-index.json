[{"title":"多线程编程入门","description":"C++ 多线程编程基础，包括线程创建、互斥锁、条件变量与原子操作","url":"Computer/CPP/Multithread/01.html","content":"# 多线程编程入门\n多线程编程是一个非常强大的功能，现代 CPU 通常有多个核心，如果你不使用多线程编程那么所有的代码都会跑在同一个 CPU 核心上，其他的核心都被闲置极大的浪费的计算资源。\n# 创建一个线程\n在 C++11 及以上版本中使用多线程编程非常简单，C++11 标准提供了强大的 std::thread\n我们可以这样创建一个线程并且传递一个函数指针作为这个线程的任务，这个函数将会在创建的子线程上运行\n#include &lt;iostream&gt;\n#include &lt;thread&gt;\n\nvoid print() { std::cout &lt;&lt; &quot;我工作在子线程上！！&quot; &lt;&lt; std::endl; }\n\nint main() {\n  std::thread t1(print);\n  std::cout &lt;&lt; &quot;主线程结束！&quot; &lt;&lt; std::endl;\n  return 0;\n}\n编译再运行后会发现并没有按照我们的心意运行\n主线程结束！\nterminate called without an active exception\n我工作在子线程上！！\n[1]    30674 IOT instruction (core dumped)  ./a.out\n这是因为我们将任务交给子线程后主线程还会继续执行，子线程的创建和调用还会使用很多时间，但是主线程依旧继续执行并结束，子线程依旧在后台执行。   通常情况下主线程的生命周期都会比子线程长，所以我们需要让子线程等待子线程执行完毕再继续执行下面的代码，这时候我们可以这么写。\n#include &lt;iostream&gt;\n#include &lt;thread&gt;\n\nvoid print() { std::cout &lt;&lt; &quot;我工作在子线程上！！&quot; &lt;&lt; std::endl; }\n\nint main() {\n  std::thread t1(print);\n  t1.join();\n  std::cout &lt;&lt; &quot;主线程结束！&quot; &lt;&lt; std::endl;\n  return 0;\n}\n此时再执行会发现正常\n我工作在子线程上！！\n","excerpt":"# 多线程编程入门\n多线程编程是一个非常强大的功能，现代 CPU 通常有多个核心，如果你不使用多线程编程那么所有的代码都会跑在同一个 CPU 核心上，其他的核心都被闲置极大的浪费的计算资源。\n# 创建一个线程\n在 C++11 及以上版本中使用多线程编程非常简单，C++11 标准提供了强大的 std::thread\n我们可以这样创建一个线程并且传递一个函数指针作为这个线程的任务，这个函数将会在创建的..."},{"title":"生产消费者模型","description":"由于不同线程处理任务和生产任务的速度是不一致的，比如我一秒产生了100个任务，但是一秒只能处理10个任务，那么产生任务和处理任务的速度匹配不上，代办任务只会越来越多导致系统崩溃不同线程处理任务和生产任务的速度是不一致的，比如我一秒产生了100个任务，但是一秒只能处理10个任务，那么产生任务和处理任务的速度匹配不上，代办","url":"Computer/CPP/Multithread/02.html","content":"# 生产消费者模型\n由于不同线程处理任务和生产任务的速度是不一致的，比如我一秒产生了100个任务，但是一秒只能处理10个任务，那么产生任务和处理任务的速度匹配不上，代办任务只会越来越多导致系统崩溃不同线程处理任务和生产任务的速度是不一致的，比如我一秒产生了100个任务，但是一秒只能处理10个任务，那么产生任务和处理任务的速度匹配不上，代办任务只会越来越多导致系统崩溃。\n所以为了解决这个问题就诞生了生产消费模型  \n生产消费模型有以下特点:\n任务队列：用于堆放代办任务生产者：用于生产任务消费者：用于处理任务\n实现的关键挑战\n互斥：同一时刻只能有一个线程操作队列，防止数据损坏。队列满时，生产者必须等待，不能撑爆内存。队列空时，消费者必须等待，不能空转 CPU。当状态改变（有新任务或有空位）时，及时唤醒等待的线程。\n据此我们实现\n\n#include &lt;condition_variable&gt;\n#include &lt;iostream&gt;\n#include &lt;mutex&gt;\n#include &lt;queue&gt;\n#include &lt;thread&gt;\n\nclass MessageQueue {\npublic:\n  void push(int val) {\n    // 保证此时只有一人能推送数据\n    std::unique_lock&lt;std::mutex&gt; lock(m_mutex);\n\n    // 只有当队列不满的时候并且没有结束的时候能推送数据\n    m_cv.wait(lock, [this] { return m_queue.size() &lt; m_maxSize || m_done; });\n\n    if (m_done) {\n      return;\n    }\n\n    m_queue.push(val);\n\n    std::cout &lt;&lt; &quot;生产了一个数：&quot; &lt;&lt; val &lt;&lt; &quot; 当前还剩&quot;\n              &lt;&lt; m_maxSize - m_queue.size() &lt;&lt; &quot; 个位置&quot; &lt;&lt; std::endl;\n    // 通知所有人任务队","excerpt":"# 生产消费者模型\n由于不同线程处理任务和生产任务的速度是不一致的，比如我一秒产生了100个任务，但是一秒只能处理10个任务，那么产生任务和处理任务的速度匹配不上，代办任务只会越来越多导致系统崩溃不同线程处理任务和生产任务的速度是不一致的，比如我一秒产生了100个任务，但是一秒只能处理10个任务，那么产生任务和处理任务的速度匹配不上，代办任务只会越来越多导致系统崩溃。\n所以为了解决这个问题就诞生了..."},{"title":"Dart基础语法","description":"Dart的基础语法、变量、常用数据类型、流程控制，类，泛型，异步等","url":"Computer/Flutter/01.html","content":"# Dart基础语法\n建议有其他编程语言基础的阅读\n# 变量声明\nvar age = 20;// 类型确定\n// age = &quot;ddd&quot;; 错误\n# 常量声明\n# const\nconst pi = 3.1415926; // 编译时确定，不能被修改\n\n// const 表达式中不能有变量值\nvar num1 = 3.1415926;\n// const pi = num1; 错误，如果行则 pi 的值是可变的，与常量的定义不符\n# final\nfinal time = DataTime.now(); // 运行时确定，不可以被修改\n# 常用数据类型\n# String\nString text = &quot;啊啊啊啊啊&quot;;\nString content = &quot;当前时间是 ${DataTime.now()}, $text&quot;;\n# int/num/double\n// int\nint count = 3;\n// num\nnum number = 3;\nnumber = 3.14;\n// double\ndouble money = 3.14;\n\n// count = money // 错误\ncount = money.toInt();\nmoney = count.toDouble();\n\n// money = number // 错误\nmoney = number.toDouble();\nnumber = money;\n# bool\nbool flag = true;\nflag = false;\n# List\nList students = [&quot;A&quot;,&quot;B&quot;,&quot;C&quot;];\n\nstudents.add(&quot;D&quot;); // 向 students 后添加 &quot;D&quot;\nstudents.addAll([&quot;E&quot;,&quot;F&quot;]); // 向 students 后添加 &quot;E&quot; , &quot;F&quot;\nstudents.remove(&quot;A&quot;); // 删除 students 中第一个(索引值最小)匹配内容的一项\nstudents.removeLast(); // 删除 s","excerpt":"# Dart基础语法\n建议有其他编程语言基础的阅读\n# 变量声明\nvar age = 20;// 类型确定\n// age = &quot;ddd&quot;; 错误\n# 常量声明\n# const\nconst pi = 3.1415926; // 编译时确定，不能被修改\n\n// const 表达式中不能有变量值\nvar num1 = 3.1415926;\n// const pi = num1; 错误..."},{"title":"Rust 入门","description":"Rust 基本语法入门，涵盖变量、所有权、引用、控制流、结构体、枚举与 Trait","url":"Computer/Rust/01.html","content":"# Rust 入门\n# 变量\n通过 let 在 rust 中变量默认是 不可变 的。如果要声明一个可变变量需要使用 mut 关键字\nfn main() {\n    let name = &quot;Rust 萌新&quot;;\n    \n    let mut energy = 100;\n    energy = 90;\n\n    const MAX_LEVEL: u32 = 99;\n\n    println!(&quot;玩家：{}, 体力: {}, 最高等级: {}&quot;, name, energy, MAX_LEVEL);\n}\n# 所有权\n所有权是 rust 中的核心概念。简单来说就是：一个堆区值在同一时间下只能由同一个\"人\"所有，当这个\"人“离开作用域后，该值就会被丢弃。栈区会自动发生拷贝，而堆区的值会发生移动。\nfn main() {\n    let s1 = String::from(&quot;hello&quot;);\n    \n    // 将S tring::from(&quot;hello&quot;); 的所有权从 s1 转移至s2 \n    let s2 = s1; \n\n    // println!(&quot;{}&quot;, s1); // 报错！因为 s1 的所有权已经给了 s2\n    println!(&quot;{}&quot;, s2);   // 正常运行\n}\n# 引用与借用\n但如果我们每次传参都要移动所有权，那代码就太难写了。引用(或者叫借用)允许你使用值但不取得所有权。\n引用有以下规则： 同时只能有一个可变引用&amp;mut T 可以同时有多个不可变引用&amp;T\nfn get_length(str: &amp;String) -&gt; usize{\n    str.len()\n}// 离开作用域后str不再指向s1的内存\n\nfn main() {\n   let s1 = String::from(&quot;hello&quot;);\n\n   // 使用 &amp; 传递不可变引用\n   let len = get_length(&amp;s1);\n\n   println!(&quot;{} 的长度是 {}&quot;,s1,len);\n}\n# 控制流与模式匹配\nRust 的 if 是表达式，而 mat","excerpt":"# Rust 入门\n# 变量\n通过 let 在 rust 中变量默认是 不可变 的。如果要声明一个可变变量需要使用 mut 关键字\nfn main() {\n    let name = &quot;Rust 萌新&quot;;\n    \n    let mut energy = 100;\n    energy = 90;\n\n    const MAX_LEVEL: u32 = 99;\n\n    p..."},{"title":"OpenGL","description":"OpenGL简介、核心模式与立即渲染模式、状态机概念","url":"Computer/CG/OpenGL/GettingStarted/OpenGL.html","content":"# OpenGL\nLearnOpenGL中文\nOpenGL不是一个库，也不是一个 API 而是一个由Khronos组织制定并维护的规范。\nOpenGL规范了每个函数的定义，具体实现交给了显卡生产商，显卡生产商需要根据OpenGL规范实现具体的函数，这里可以找到OpenGL3.3的规范文档。\n# 核心模式与立即渲染模式\n在OpenGL3.2之前OpenGL是使用立即渲染模式（固定管线，程序员不能自定义管线），在立即渲染模式下绘制图形很简单，OpenGL对底层细节做了很大程度的抽象，与此同时灵活性也大大被限制住了。后期程序员需要更多的灵活性，所以从OpenGL3.2开始，规范文档开始废除立即渲染模式，开启了核心模式的时代。\n核心模式允许程序员能自定义管线（顶点着色器，片段着色器等），更多的底层控制，与此同时复杂性也提升了不少。\n# 状态机\nOpenGL是一个状态机：通过一系列变量描述OpenGL此刻该如何运行。OpenGL的状态被称为上下文。我们通过改变状态机的变量来渲染。\n假设我们想绘制一个三角形边框，而不是三角形面我们可以设置状态机，让其去绘制线框而不是面。\n我们通常是使用状态设置函数去改变上下文状态。\n# 对象\nOpenGL是使用C语言写的，所以可以把对象看作一个C语言结构体。","excerpt":"# OpenGL\nLearnOpenGL中文\nOpenGL不是一个库，也不是一个 API 而是一个由Khronos组织制定并维护的规范。\nOpenGL规范了每个函数的定义，具体实现交给了显卡生产商，显卡生产商需要根据OpenGL规范实现具体的函数，这里可以找到OpenGL3.3的规范文档。\n# 核心模式与立即渲染模式\n在OpenGL3.2之前OpenGL是使用立即渲染模式（固定管线，程序员不能自定..."},{"title":"创建窗口","description":"使用GLFW和GLAD搭建OpenGL开发环境","url":"Computer/CG/OpenGL/GettingStarted/CreateWindow.html","content":"# 创建窗口\n这里使用与LearnOpenGL不同的方式，LearnOpenGL使用Visual Studio，而我们使用命令行+CMake\nOpenGL的画面不能直接渲染在你的屏幕上，一般情况下只能渲染在操作系统提供的桌面窗口上。\n那么由于有多种操作系统的存在，每个操作系统提供的接口也不一样，所以OpenGL并没有能够直接处理窗口的能力，OpenGL将这些操作抽象出去，让我们自行处理窗口。我们可以手动处理，也可以使用别人封装好的库，这里使用GLFW。\n# GLFW\nGLFW提供定义窗口参数以及处理用户输入的能力。\n# GLAD\n因为OpenGL是一个规范，具体的实现是由显卡厂商提供的，由于OpenGL驱动版本众多，它大多数函数的位置都无法在编译时确定下来，需要在运行时查询。开发者需要在运行时获取函数地址并将其保存在一个函数指针中供以后使用。但是这已经有人为我们做了，我们使用GLAD这个库来加载所有OpenGL相关的函数。\n打开GLAD的在线服务，将语言(Language)设置为C/C++，在API选项中，选择3.3以上的OpenGL(gl)版本（我们的教程中将使用3.3版本，但更新的版本也能用）。之后将模式(Profile)设置为Core，并且保证选中了生成加载器(Generate a loader)选项。现在可以先（暂时）忽略扩展(Extensions)中的内容。都选择完之后，点击生成(Generate)按钮来生成库文件。\n这时解压这个zip压缩包，获得glad这个文件夹，记住这个文件夹的目录假设为(C:\\Users\\admin\\Downloads\\glad)\n# 创建项目结构\nmkdir learn_opengl\ncd learn_opengl\nmkdir thrid_party\ncd thrid_party\ngit clone https://github.com/glfw/glfw.git\ncp -r C:\\Users\\admin\\Downloads\\glad .\ncd ..\n这时候我们需要的第三方库已经准备好了\n下一节将介绍如何使用glfw和glad创建一个窗口和渲染主循环，以及提供完整的CMake项目初始化代码","excerpt":"# 创建窗口\n这里使用与LearnOpenGL不同的方式，LearnOpenGL使用Visual Studio，而我们使用命令行+CMake\nOpenGL的画面不能直接渲染在你的屏幕上，一般情况下只能渲染在操作系统提供的桌面窗口上。\n那么由于有多种操作系统的存在，每个操作系统提供的接口也不一样，所以OpenGL并没有能够直接处理窗口的能力，OpenGL将这些操作抽象出去，让我们自行处理窗口。我们可..."},{"title":"你好，窗口","description":"创建第一个OpenGL窗口与渲染循环","url":"Computer/CG/OpenGL/GettingStarted/HelloWindow.html","content":"# 你好，窗口\n新建一个存放源码的目录\nmkdir src\ncd src\n新建一个main.cpp文件\n写入如下代码\n#include &lt;glad/glad.h&gt;\n#include &lt;GLFW/glfw3.h&gt;\n\nint main()\n{\n    glfwInit();\n    glfwWindowHint(GLFW_CONTEXT_VERSION_MAJOR, 3);\n    glfwWindowHint(GLFW_CONTEXT_VERSION_MINOR, 3);\n    glfwWindowHint(GLFW_OPENGL_PROFILE, GLFW_OPENGL_CORE_PROFILE);\n    //glfwWindowHint(GLFW_OPENGL_FORWARD_COMPAT, GL_TRUE);\n\n    return 0;\n}\nCAUTION\nGLAD必须包含在GLFW之前，因为如果GLFW包含在前他会去系统找 OpenGL 头文件（通常是老版本），在这之后在包含GLAD就会触发重定义问题。\n在main函数中调用glfwInit函数来初始化GLFW，然后使用glfwWindowHint函数来配置GLFW，\n第一个参数代表选项的名称，我们可以从很多以GLFW_开头的枚举值中选择；第二个参数接受一个整型，用来设置这个选项的值。\n上述代码中表达的意思是使用OpenGL3.3，并且使用OpenGL核心模式。\n# 创建一个窗口对象\nGLFWwindow* window = glfwCreateWindow(800, 600, &quot;LearnOpenGL&quot;, NULL, NULL);\nif (window == NULL)\n{\n    std::cout &lt;&lt; &quot;Failed to create GLFW window&quot; &lt;&lt; std::endl;\n    glfwTerminate();\n    return -1;\n}\nglfwMakeContextCurrent(window);\nglfwCreateWindow()前两个参数是窗口的宽高，第三个窗口是窗口标题，最后两个参数暂时忽略。这个函数将会返回一个GLFWwindow对象。\n如果窗口创建失败，使用glfwTe","excerpt":"# 你好，窗口\n新建一个存放源码的目录\nmkdir src\ncd src\n新建一个main.cpp文件\n写入如下代码\n#include &lt;glad/glad.h&gt;\n#include &lt;GLFW/glfw3.h&gt;\n\nint main()\n{\n    glfwInit();\n    glfwWindowHint(GLFW_CONTEXT_VERSION_MAJOR, 3);..."},{"title":"你好，三角形","description":"图形渲染管线、着色器编写与顶点缓冲对象","url":"Computer/CG/OpenGL/GettingStarted/HelloTriangle.html","content":"# 你好，三角形\n在OpenGL中，任何事物都在3D空间中，但是屏幕和窗口是2D的，3D坐标转为2D坐标的处理过程是由OpenGL的图形渲染管线管理的，通常管线可以被划分为两个主要的部分：将3D坐标转换为2D坐标；将2D坐标转变化实际的颜色像素。\n图形渲染管线接受一组3D坐标，然后把它们转变为你屏幕上的有色2D像素输出。\n图形渲染管线可以被划分为几个阶段，每个阶段将会把前一个阶段的输出作为输入。\n这些阶段被叫做着色器，程序员可以通过编写着色器程序来自定义渲染管线，从而渲染出精致的图形。\n\n正如这张图所示，OpenGL会将顶点数据输送给管线的第一个阶段：顶点着色器，顶点着色器主要是进行各种矩阵变换，将顶点变换到你期望的位置，进行一数据传输等等，再进入第二阶段几何着色器，几何着色器可以将一个图元转化为另一个图元，例如将一个点输出为一个正方形等等。再进入第三个阶段，进行图元装配，将不同的点按顺序连接好，这个阶段是不可编程的，第四个阶段光栅化，将集具体的坐标点光栅化为像素，片段着色器，对每个像素进行具体的上色操作，最后进行测试与混合\n# 顶点着色器\n我们先使用一个非常简单的Vertex Shader程序，在慢慢的接触的途中你自然能领悟到其中的写法了。\n#version 330 core //表示使用 opengl3.3 核心模式\nlayout (location = 0) in vec3 aPos;// 表示位置0是输入变量aPos\n\nvoid main()\n{\n    // glsl会将gl_Position作为程序的输出，由于我们的输入是一个3分量的向量，我们必须把它转换为4分量的\n    gl_Position = vec4(aPos.x, aPos.y, aPos.z, 1.0);\n}\n现在，我们暂时将顶点着色器的源代码硬编码在代码文件顶部的C风格字符串中：\nconst char *vertexShaderSource = &quot;#version 330 core\\n&quot;\n    &quot;layout (location = 0) in vec3 aPos;\\n&quot;\n    &quot;void main()\\n&quot;\n    &quot;{\\n&quot;\n    &quot;   gl_Position = vec4(aPos.x","excerpt":"# 你好，三角形\n在OpenGL中，任何事物都在3D空间中，但是屏幕和窗口是2D的，3D坐标转为2D坐标的处理过程是由OpenGL的图形渲染管线管理的，通常管线可以被划分为两个主要的部分：将3D坐标转换为2D坐标；将2D坐标转变化实际的颜色像素。\n图形渲染管线接受一组3D坐标，然后把它们转变为你屏幕上的有色2D像素输出。\n图形渲染管线可以被划分为几个阶段，每个阶段将会把前一个阶段的输出作为输入。..."},{"title":"实数","description":"实数集的稠密性与连续性，戴德金分割","url":"Math/Calculus/01.html","content":"# 实数\n实数集 R 区别于其他数集的最核心的两个特征是：稠密性 与 连续性。\n# 稠密性\n定义： 在实数轴上，任意两个不相等的实数之间，必然存在另一个实数。\n事实上，不仅存在一个，而是存在无穷多个实数。这一性质说明实数在直线上是“处处存在”的，不存在孤立的点。\n# 连续性\n定义： 实数轴是“完备”的，即数轴上不存在任何“洞”。\n有理数的局限性： 有理数集虽然也具有稠密性（任意两个有理数之间仍有有理数），但它不具备连续性。如果我们只看有理数，数轴上会布满密密麻麻的“洞”，这些洞对应的正是无理数。\n# 连续性的判定：戴德金分割\n我们可以通过“戴德金分割”这一思想来直观理解连续性的差异：\n# 实数的表现：\n如果在实数轴上随机进行一次“分划”，这一刀必然会落在某个具体的实数上。这个数要么属于左半部分，要么属于右半部分。这说明实数轴是紧密相连的。\n# 有理数的表现：\n对于有理数集，我们可以构造一种特殊的切法。例如，将所有有理数按如下规则分类：\nA组： 所有平方小于2的正有理数（及负有理数）；B组： 所有平方大于2的正有理数。\n当我们试图寻找这一“刀”切在哪个有理数上时，会惊奇地发现：在有理数范围内，根本没有这个数，这就说明有理数是不完备的，他并不连续。","excerpt":"# 实数\n实数集 R 区别于其他数集的最核心的两个特征是：稠密性 与 连续性。\n# 稠密性\n定义： 在实数轴上，任意两个不相等的实数之间，必然存在另一个实数。\n事实上，不仅存在一个，而是存在无穷多个实数。这一性质说明实数在直线上是“处处存在”的，不存在孤立的点。\n# 连续性\n定义： 实数轴是“完备”的，即数轴上不存在任何“洞”。\n有理数的局限性： 有理数集虽然也具有稠密性（任意两个有理数之间仍有有..."}]